import { Button, Input } from "antd";
import styled from "styled-components";

export const ChatInput = styled(Input.TextArea)`
    padding: 12px 24px;
    border-radius: 20px;
`;

export const SendButton = styled(Button)`
    width: 50px;
`;